# https://github.com/hadolint/hadolint#configure
# Образовательные Dockerfile’ы: допускаем типичные упрощения; жёсткий контроль CVE остаётся в Trivy.
ignored:
  - DL3002  # nginx:alpine в финальном слое фронта — стандартная отдача статики
  - DL3007  # мажорные теги базовых образов; актуальность слоёв — отдельно в CI (Trivy)
  - DL3008  # apt без pin версий в минимальном слое api
